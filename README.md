# 1. INSTALL
```txt
pkg install git
git clone https://github.com/PepaPig-design/Clobe.git
pkg update && pkg upgrade -y
pkg install python -y
pkg install chromium chromedriver -y
pkg install binutils rust make -y
pkg update
pkg install tur-repo -y
cd Clobe
pip install selenium pyfiglet chromedriver-autoinstaller x11-repo -y 
```
# Run
```txt
python the-scorphx-algo.py
```
